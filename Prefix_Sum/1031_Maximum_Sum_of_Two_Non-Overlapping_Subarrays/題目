＃難度： Medium

＃題目說明： 
- 在 array nums 裡取兩段連續的 subarray 其元素合要是最大的
- 兩段連續的 subarray 長度分別是 firstLen 和 secondLen


＃ Constraints
1 <= firstLen, secondLen <= 1000
2 <= firstLen + secondLen <= 1000
firstLen + secondLen <= nums.length <= 1000
0 <= nums[i] <= 1000

＃例子
Input: cardPoints = [1,2,3,4,5,6,1], k = 3
Output: 12，從後面挑 1 6 5 

＃想法
- 創建 prefix[i] 表示從頭取 i 個元素的總合是多少
- 用 prefix 的好處是做一次加總就好
- 再來就用兩個 for 把所有可能跑過

＃ 時間複雜度
O(n^2)




    


 


